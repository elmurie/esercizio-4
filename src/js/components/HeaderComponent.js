import Component from '../abstracts/Component';
import 'ScssComponents/c-header-component';

export default class HeaderComponent extends Component {
	constructor(compEl) {
		super(compEl);
	}

	init() {
		super.init();
	}
}
